apiVersion: v1
kind: Service
metadata:
  name: frontend-dev
  namespace: chat-dev
  labels:
    app: frontend
    env: dev
spec:
  type: ClusterIP
  selector:
    app: frontend
    env: dev
  ports:
    - port: 80
      targetPort: 5173
